<%= form_for(@hookah) do |f| %>
  <% if @hookah.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hookah.errors.count, "error") %> prohibited this hookah from being saved:</h2>

      <ul>
      <% @hookah.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
